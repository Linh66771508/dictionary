import 'package:flutter/material.dart';

class SynonymManagementScreen extends StatelessWidget {
  const SynonymManagementScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Quản lý đồng nghĩa nằm trong màn Quản lý từ vựng.'),
    );
  }
}
