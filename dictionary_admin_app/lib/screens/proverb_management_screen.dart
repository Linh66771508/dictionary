import 'package:flutter/material.dart';

class ProverbManagementScreen extends StatelessWidget {
  const ProverbManagementScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Quản lý tục ngữ nằm trong màn Quản lý từ vựng.'),
    );
  }
}
